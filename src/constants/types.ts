export type ProjectType = {
  name: string;
  description: string;
  cover?: string;
  technologies?: string[];
};

export type TestimonialType = {
  author: string;
  content: string;
  job: string;
  picture: string;
};
